all: 
	cargo build --release || cargo build --release || cargo build --release || cargo build --release || cargo build --release || cargo build --release || cargo build --release || cargo build --release || cargo build --release
	mv target/release/webcrawler . 